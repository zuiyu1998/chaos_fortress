# Role

`Role` 是一个组件（Component），用于标记一个实体为"角色"。

## 用途

- 持有 `Role` 组件的实体代表一个可操控的角色单位（玩家方）。
- 每个角色占据一个地图格子，拥有完整的属性、技能和状态。
- 通过 `Role` 组件可在 ECS 查询中快速筛选所有角色实体。


## 与角色系统的关系

`Role` 组件是 `role_system.md` 中定义的角色数据模型的 ECS 实现载体。角色的属性（生命、攻击、防御等）通过附加的组件表达，行为由对应的 System 驱动。
